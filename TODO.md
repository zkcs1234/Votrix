# TODO

- [ ] Update `frontend/src/components/ui/Button.jsx` so button text color is always readable and cannot be overridden by accidental muted/invisible classes.
- [x] Add a small, targeted CSS rule in `frontend/src/index.css` to ensure native `<button>` elements that inherit wrong color remain readable (without breaking icon-only buttons).
- [ ] Run frontend build/lint (if available) and verify visually the organizer/create buttons.
